<!DOCTYPE html>
<html lang="en">
<%= render "shared/components/header" %>
<body class="h-full">
<%= turbo_frame_tag "flash_message"do %>
  <%= render "shared/flash" %>
<% end %>
<main class="body-container h-full ">
  <div class="flex min-h-full">
    <div class="flex relative flex-1 flex-col px-6 py-12
                sm:px-8
                lg:justify-center lg:flex-none lg:px-20
                xl:px-24
                2xl:px-36"
    >
     <div class="w-full text-center mb-6
                lg:absolute lg:top-12 lg:text-left lg_mb-0"
     >
       <%= link_to "/", :class => '' do %>
       Logo here
       <% end %>
     </div>
      <div class="w-full max-w-sm mx-auto lg:w-96 ">
        <h2 class="text-center mb-12 text-3xl lg:text-left lg:mb-8">
          <%= yield(:title) %>
        </h2>
        <%= yield %>
      </div>
    </div>
    <div class="bg-gray-50 dark:bg-gray-900 relative hidden w-0 flex-1 lg:block">

      <% if controller_name == 'registrations' && action_name == new %>
      <div class="absolute z-50 w-full h-full flex-col items-center justify-center">
        <div class="flex h-full flex-col items-center justify-center">
          <div class="mx-auto bg-white dark:bg-gray-900 bg-opacity-30 dark:bg-opacity-80 text-gray-800 dark:text-gray-900">
            <div class="italic font-medium">&ldquo; This is testimonial quote. You should sign up </div>
            <div class="text-sm text-right pr-8 text-gray-400 font-medium">
              <span>Someone's name</span>
            </div>
          </div>
        </div>
      </div>
      <% end %>
      <div class="absolute z-40 top-0 left-0 w-full h-full bg-gray-600 dark:bg-black bg-opacity-40">
<!--        <img src="" alt="" class="">-->
      </div>
    </div>
  </div>
</main>
</body>
